name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping FocusFrame
        run: curl -sS -o /dev/null https://focus-frame.onrender.com
